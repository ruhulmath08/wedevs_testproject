import 'package:flutter/material.dart';

class ListGridTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ListGrid Tab'),
    );
  }
}
