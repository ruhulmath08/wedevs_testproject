//home_tab
enum FetchJsonDataState { initialState, fetchJsonData, fetchJsonDataSuccess, fetchJsonDataFailed }

//for authentication
enum AuthenticationStatus { unknown, authenticated, unauthenticated }

//for token
enum TokenStatus { expired, validate }
